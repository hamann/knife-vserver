Vagrant::configure("2") do |config|
  config.vm.define 'host' do |box_config|
    box_config.vm.box = 'host'
    box_config.vm.box_url = 'https://s3-eu-west-1.amazonaws.com/sauspiel-vagrant/vbox/debian-wheezy.box'
  end
end
